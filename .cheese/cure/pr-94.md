status: ok
next: done
artifact: .cheese/affinage/pr-94.md
Cured three contained PR #94 review findings; deferred one structural pr_stack limitation.

# Cure Report — PR #94

## Applied

- **[from-comment:3328078861] [correctness:high]**: Restored `prompt_prepend=config.planning_prompt_prepend` when constructing `Planner` from `milknado plan`, with a regression test covering the configured prompt prepend.
- **[from-comment:3328078905] [correctness:high]**: Limited `.mcp.json` `--mcp-config` injection to Claude workers only, with a regression test proving `codex` does not receive the Claude-only flag.
- **[from-comment:3328078890] [complexity:medium]**: Split issue/spec materialization helpers from `cli_plan.py` into `cli_plan_specs.py`; `cli_plan.py` is now 255 lines.

## Deferred

- **[from-comment:3328078879]**: Deferred. The pr_stack branch-base concern is valid but structural; fixing it requires changing how dependent worktree branches are rebased or merged during execution, not a localized review fix.

## Checks

- `uv run pytest tests/test_cli.py::TestPlanSpecOption::test_passes_planning_prompt_prepend tests/test_cli.py::TestPlanIssueOption tests/test_mcp_server.py::TestMcpConfigInjection -q`: pass, 15 passed.
- `just build`: pass, 1191 passed, 4 skipped, coverage 95.31%.

## Re-review

- Remaining risk: `pr_stack` remains documented as sound only for file-independent nodes.
- Suggested next step: merge PR #94 after the remote build, if required checks run cleanly.
