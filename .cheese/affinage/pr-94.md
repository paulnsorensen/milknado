status: ok
next: done
artifact: .cheese/cure/pr-94.md
PR #94 had four current Copilot inline comments; three were cured, one structural pr_stack limitation was deferred with a drafted reply.

# Affinage Report — PR #94

## Orientation

PR #94 (`ralphify/03-refactors` -> `main`) refactors CLI/execution internals and adds crash recovery plus opt-in `pr_stack`. This run graded four current Copilot inline comments, materialized the merge from `origin/main`, cured the contained findings, and ran the project build gate.

## PR status

- Build: GitHub reported no checks on the PR branch before cure; local `just build` passed after cure.
- Merge: GitHub reported `DIRTY`; local `git merge --no-edit origin/main` completed cleanly and created a merge commit.
- Comments: 4 current Copilot inline comments, 0 skipped as outdated.
- Fresh review: existing `.cheese/affinage/pr-94.md` had prior fresh-review findings; this run only cured the comment-sourced contained set and deferred the structural pr_stack item.

## High

- **[from-comment:3328078861] [correctness:high]** Copilot on `src/milknado/cli_plan.py` — `Planner(...)` stopped receiving `config.planning_prompt_prepend`, so configured planning prompt prepends were ignored by `milknado plan`.
  - location: contract · fix-cost-now: contained · fix-cost-later: contained
  - recommendation: pass `prompt_prepend=config.planning_prompt_prepend` into `Planner(...)` and cover it with a CLI regression test.

- **[from-comment:3328078905] [correctness:high]** Copilot on `src/milknado/domains/dispatch/runner.py` — `_inject_mcp_config` appended Claude's `--mcp-config` flag to every allowed worker executable, including `codex`, `cursor-agent`, and `gemini`.
  - location: contract · fix-cost-now: contained · fix-cost-later: contained
  - recommendation: append the flag only when the resolved executable basename is `claude`, and add a non-Claude regression test.

## Medium

- **[from-comment:3328078890] [complexity:medium]** Copilot on `src/milknado/cli_plan.py` — the refactored module was 413 lines, above the project's 300-line file limit.
  - location: module · fix-cost-now: contained · fix-cost-later: contained
  - recommendation: move issue/spec materialization helpers into a sibling CLI module. `src/milknado/cli_plan.py` is now 255 lines and `src/milknado/cli_plan_specs.py` is 161 lines.

## Reviewer-rejected

- **[from-comment:3328078879]** Copilot on `src/milknado/domains/execution/pr_stack.py` — stacked PRs change PR bases without rebasing dependent head branches onto prerequisite branches.
  - reason: valid limitation, but not a contained fix. `open_stacked_prs` already documents that `pr_stack` stacks PR bases but not underlying branches and is sound only for file-independent nodes. Correcting this requires an execution-time branch rebase/merge design across executor, run loop, and pr_stack.
  - draft reply: "This is a known, documented limitation in `open_stacked_prs`: it stacks PR bases but does not rebase the underlying branches, so `pr_stack` is currently sound only for file-independent nodes. Fixing that means changing execution-time branch staging across executor/run_loop/pr_stack, which is beyond this review fix. Leaving this as follow-up rather than growing PR #94."

## Confidence

`<certain>` — the three cured findings were verified by source reads, regression tests, and `just build`; the pr_stack item is deferred because the code itself documents the limitation and a contained fix is not present.

## Next step

Done after cure: commit, push, and post review-thread replies.
